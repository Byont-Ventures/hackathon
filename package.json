{
  "name": "byont-web3-workshop",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "format": "prettier --loglevel warn --check .",
    "format:fix": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "prettier-plugin-jsdoc": "^0.4.2",
    "prettier-plugin-solidity": "^1.0.0-beta.24"
  }
}
